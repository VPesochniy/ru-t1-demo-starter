package ru.t1.demo.config;

//    здесь может быть более глубокий уровень детализации
//    public enum LogLevel {FATAL, ERROR, WARN, INFO, DEBUG, TRACE}
//    каждый следующий уровень детализации включает предыдущий


public enum LogLevel {
    FATAL,
    ERROR,
    WARN,
    INFO
}
